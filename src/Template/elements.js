import styled from "styled-components";

export const Container = styled.div`
    min-width: 100%;
    min-height: 100vh;
    background: ${({theme}) => theme.secondaryBackground};
    padding: 160px 20px 20px 385px;
`